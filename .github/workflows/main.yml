name: Run Windows Command

on:
  push:
    branches:
      - main

jobs:
  run-command:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run Windows command
      run: |
        # Replace the following line with your Windows command
        echo "Hello, World!"
